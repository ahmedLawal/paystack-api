/**
 * Paystack API service config
 */
export interface PSConfig {
    /**The secret key gotten from your Paystack dashboard */
    secretKey?: string;
    /**The defualt currency to use */
    currency?: string;
}
